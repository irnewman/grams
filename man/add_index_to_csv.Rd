% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{add_index_to_csv}
\alias{add_index_to_csv}
\title{Add a new index column to existing anagram CSV files.}
\usage{
add_index_to_csv(
  col_name,
  index_fn,
  output_dir = "anagram_data",
  words = NULL,
  overwrite = FALSE,
  col_type = numeric(1),
  needs_solution = FALSE
)
}
\arguments{
\item{col_name}{Name of the new column to add.}

\item{index_fn}{A function that takes a single word string and returns a
single value. The return type should match \code{col_type}.}

\item{output_dir}{Path to the directory containing CSV files. Default is
"anagram_data".}

\item{words}{Optional character vector of source words. If provided, only
the CSV files for those words are updated. If NULL, all CSV files in
\code{output_dir} are updated.}

\item{overwrite}{Logical. If TRUE, recomputes and overwrites the column
even if it already exists. Default FALSE.}

\item{col_type}{The expected return type of \code{index_fn}, specified as
a zero-length vector (e.g. \code{numeric(1)}, \code{character(1)},
\code{logical(1)}). Default \code{numeric(1)}.}

\item{needs_solution}{Logical. If \code{TRUE}, the index function receives
both the permutation and the source word as arguments. If \code{FALSE},
only the permutation is passed. Default \code{FALSE}.}
}
\value{
Invisibly returns NULL. Progress and summary messages are printed
to the console.
}
\description{
Applies a function to the permutation column of each CSV file and saves
the result as a new column. Files that already contain the column are
skipped by default, making it safe to run incrementally as new indices
are developed. Searches recursively through subdirectories.
}
