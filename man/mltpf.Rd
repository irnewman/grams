% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency.R
\name{mltpf}
\alias{mltpf}
\title{Mean Log Tri-Phone Frequency}
\usage{
mltpf(
  phoneme_string,
  cache = NULL,
  positional = TRUE,
  scope = c("by_length", "overall"),
  zero_handling = c("laplace", "na", "minimum")
)
}
\arguments{
\item{phoneme_string}{A character string of space-separated ARPAbet phonemes
(e.g. \code{"K AE1 T"}). Stress markers are stripped automatically.}

\item{cache}{Optional cache. If \code{NULL}, uses the default CMU phoneme
unigram cache.}

\item{positional}{Logical. If \code{TRUE} (default), uses positional phoneme
frequencies.}

\item{scope}{One of \code{"by_length"} (default) or \code{"overall"}. Only
applies when \code{positional = FALSE}.}

\item{zero_handling}{One of \code{"laplace"} (default), \code{"na"},
\code{"minimum"}.}
}
\value{
Numeric MLTPF score.
}
\description{
Computes the mean log positional triphone frequency (MLTPF) for an ARPAbet
phoneme sequence. Uses raw frequencies with Laplace smoothing (adding 1
before logging).
}
