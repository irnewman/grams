% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stimuli.R
\name{find_matched_unsolvable}
\alias{find_matched_unsolvable}
\title{Generate unsolvable anagram(s) matched to a target}
\usage{
find_matched_unsolvable(
  target_anagram,
  match_metrics = c("SBF"),
  tolerance = list(SBF = 0.05),
  n_swaps = 1,
  swap_type = c("either", "vowels_only", "consonants_only", "both"),
  swap_strategy = c("phonetic", "random", "weighted"),
  max_position_overlap = NULL,
  n_candidates = 1,
  max_attempts = 100,
  sig_index = grams::sig_index
)
}
\arguments{
\item{target_anagram}{Character string of the anagram to match}

\item{match_metrics}{Character vector of metrics to match (e.g., c("SBF", "Articulability"))}

\item{tolerance}{Named list of tolerances for each metric (e.g., list(SBF = 0.05, Articulability = 0.2))}

\item{n_swaps}{Number of letters to swap}

\item{swap_type}{Type of swaps: "vowels_only", "consonants_only", "either", "both"}

\item{swap_strategy}{Swap strategy: "phonetic", "random", "weighted"}

\item{max_position_overlap}{Maximum proportion of letters in same positions (NULL = no constraint)}

\item{n_candidates}{Number of unique candidates to return (default 1)}

\item{max_attempts}{Maximum number of attempts before giving up}

\item{sig_index}{Signature index for dictionary lookup used to verify
candidates are not existing words. Defaults to \code{grams::sig_index}.}
}
\value{
Data frame with candidates and their properties, or NULL if none found
}
\description{
Generate unsolvable anagram(s) matched to a target
}
