% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{generate_word_csv}
\alias{generate_word_csv}
\title{Generate and save anagram candidates with psycholinguistic indices.}
\usage{
generate_word_csv(
  word,
  output_dir = "anagram_data",
  total = 100,
  top_n = 5,
  n_iter = 5000,
  n_restarts = 200,
  overwrite = FALSE,
  sig_index = grams::sig_index
)
}
\arguments{
\item{word}{A word in string format.}

\item{output_dir}{Path to the output directory. Default is "anagram_data".}

\item{total}{Target number of permutations to generate. Default 100.}

\item{top_n}{Number of top and bottom SBF permutations to include. Default 5.}

\item{n_iter}{Number of iterations per simulated annealing restart. Default 5000.}

\item{n_restarts}{Number of simulated annealing restarts. Default 200.}

\item{overwrite}{Logical. If TRUE, regenerates and overwrites existing files.
Default FALSE.}

\item{sig_index}{Signature index for dictionary lookup. Defaults to the
package internal index.}
}
\value{
Invisibly returns the path to the saved CSV file.
}
\description{
For a given word, generates up to \code{total} permutations using
\code{generate_anagram_candidates()}, computes psycholinguistic indices
for each via \code{compute_anagram_indices()}, and saves the result as a CSV
file. Files are organized into subfolders by word length. Existing files
are skipped by default.
}
