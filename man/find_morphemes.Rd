% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphemes.R
\name{find_morphemes}
\alias{find_morphemes}
\title{Count or list morphemes embedded in a word.}
\usage{
find_morphemes(word, output = "list", morph_list = NULL)
}
\arguments{
\item{word}{A word or anagram in string format.}

\item{output}{One of "count", "list", or "indices". Default "list".}

\item{morph_list}{A data frame with a column named "morpheme". Defaults
to the package morpheme list.}
}
\value{
Numeric count if output = "count", character vector of morphemes
in left-to-right order (uppercase) if output = "list", or numeric vector
of starting positions if output = "indices".
}
\description{
Finds morphemes from a provided list that appear in the word, removing
shorter morphemes that are fully embedded within longer found morphemes
(e.g. if "pre" and "re" are both found, only "pre" is kept).
}
