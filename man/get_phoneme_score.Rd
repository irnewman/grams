% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phonemes.R
\name{get_phoneme_score}
\alias{get_phoneme_score}
\title{Compute a phonotactic score for a word, averaging across pronunciations}
\usage{
get_phoneme_score(word, score_fn = mlbpf, pronunciation = "first", ...)
}
\arguments{
\item{word}{A word or string of letters.}

\item{score_fn}{A phonotactic scoring function, e.g. \code{\link{sbpf}},
\code{\link{mlbpf}}, \code{\link{supf}}, \code{\link{mlupf}},
\code{\link{stpf}}, \code{\link{mltpf}}. Defaults to \code{\link{mlbpf}}.}

\item{pronunciation}{One of \code{"first"} (default) or \code{"all"}.
For CMU words with multiple listed pronunciations, \code{"first"} returns
only the primary pronunciation as a single string. \code{"all"} returns
every pronunciation as a character vector, leaving averaging or other
handling to the caller (see \code{\link{get_phoneme_score}}).}

\item{...}{Additional arguments passed to \code{score_fn}.}
}
\value{
A single numeric score.
}
\description{
Compute a phonotactic score for a word, averaging across pronunciations
}
