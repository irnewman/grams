% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbourhoods.R
\name{phonological_neighbours}
\alias{phonological_neighbours}
\title{Compute phonological neighbourhood measures}
\usage{
phonological_neighbours(
  phonemes,
  lexicon = NULL,
  n = 20,
  method = "lv",
  output = "both"
)
}
\arguments{
\item{phonemes}{Either a character vector of ARPAbet tokens, a
space-separated ARPAbet string, or a pre-encoded one-character string
(if \code{encoded = TRUE}).}

\item{lexicon}{A character vector of pre-encoded one-character phoneme
strings to compare against. Defaults to \code{grams::cmu$phonemes_1char}.}

\item{n}{Number of nearest neighbours for PLD-N. Default is \code{20}.}

\item{method}{Distance method passed to \code{stringdist}. Default is
\code{"lv"}.}

\item{output}{One of \code{"pld"}, \code{"ped"}, \code{"both"}, or
\code{"neighbours"}.}
}
\value{
A numeric value, a \code{data.frame} if \code{output = "both"},
or a character vector of encoded neighbour strings if
\code{output = "neighbours"}.
}
\description{
Computes Phonological Levenshtein Distance (PLD-N) and/or phonological
edit distance neighbours (PED-N) for a given word or phoneme sequence
against a reference lexicon. Phoneme sequences are encoded using the
ARPAbet one-character encoding before distance computation, so that each
character-level edit corresponds to exactly one phoneme substitution,
insertion, or deletion.
}
\seealso{
\code{\link{pld20}}, \code{\link{ped1}},
\code{\link{orthographic_neighbours}}
}
